name: flutter_patrol_test_demo
description: "A new Flutter project."

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.8.0

dependencies:
  cupertino_icons: ^1.0.8
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter
  patrol: ^3.19.0

flutter:
  uses-material-design: true

patrol:
  app_name: Patrol Demo
  android:
    package_name: com.example.patrol.demo
  ios:
    bundle_id: com.example.patrol.demo
